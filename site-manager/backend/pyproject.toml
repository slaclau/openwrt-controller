[project]
name = "openwrt-site-manager"
version = "0.0.1"
dependencies = [
    "authlib",
    "fastapi[standard]",
    "httpx2",
    "itsdangerous",
    "joserfc",
    "pwdlib[argon2]",
    "pyjwt",
    "sqlmodel",
]

[tool.fastapi]
entrypoint = "app.main:app"