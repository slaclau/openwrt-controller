[project]
name = "openwrt-site-manager"
version = "0.0.1"
dependencies = [
    "authlib",
    "fastapi[standard]",
    "httpx2",
    "itsdangerous",
    "joserfc",
    "pyjwt",
    "sqlmodel",
]

[tool.fastapi]
entrypoint = "app.main:app"