[tasks.dev]
run = "fastapi dev --host 0.0.0.0 --port 8000"

[tasks.build]
run = "uv build --sdist"

[tasks.clean]
run = "rm -rf openapi.json"

[tasks.format]
run = "uv run black app"

[tasks.generate-openapi-spec]
run = "python -m gen_openapi"
