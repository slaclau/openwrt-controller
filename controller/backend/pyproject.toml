[project]
name = "openwrt-controller"
version = "0.0.1"
dependencies = [
    "aiortc",
    "fastapi[standard]",
    "httpx2",
    "netjsonconfig",
    "sqlmodel",
]

[tool.fastapi]
entrypoint = "app.main:app"