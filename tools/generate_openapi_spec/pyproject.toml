[project]
name = "generate-openapi-spec"
dynamic = ["version"]

[project.scripts]
gen-openapi = "gen_openapi:main"

[build-system]
requires = ["GitPython", "setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["gen_openapi"]
